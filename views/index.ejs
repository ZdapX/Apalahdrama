<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DramaBox - Nonton Drama Pendek</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background-color: #000; color: white; }
        .drama-card:hover img { transform: scale(1.05); }
    </style>
</head>
<body class="p-4 md:p-8">
    <header class="flex justify-between items-center mb-8">
        <h1 class="text-2xl font-bold text-pink-600">Drama<span class="text-white">Box</span></h1>
        <div class="relative">
            <input type="text" placeholder="Cari drama..." class="bg-gray-900 border border-gray-700 rounded-full px-4 py-1 text-sm focus:outline-none focus:border-pink-500">
        </div>
    </header>

    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4">
        <% if (dramas && dramas.length > 0) { %>
            <% dramas.forEach(drama => { %>
                <a href="/watch/<%= drama.id %>" class="drama-card block group">
                    <div class="relative aspect-[3/4] overflow-hidden rounded-xl bg-gray-800">
                        <img src="<%= drama.thumbnail %>" alt="<%= drama.title %>" class="w-full h-full object-cover transition-transform duration-300">
                        <div class="absolute bottom-2 right-2 bg-pink-600 text-[10px] px-2 py-0.5 rounded-md font-bold">
                            HD
                        </div>
                    </div>
                    <h3 class="mt-2 text-sm font-semibold truncate group-hover:text-pink-500"><%= drama.title %></h3>
                </a>
            <% }); %>
        <% } else { %>
            <p class="col-span-full text-center text-gray-500">Tidak ada drama ditemukan.</p>
        <% } %>
    </div>
</body>
</html>
