<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Watching - DramaBox</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white">
    <div class="max-w-md mx-auto h-screen flex flex-col">
        <!-- Header Nonton -->
        <div class="p-4 flex items-center gap-4">
            <a href="/" class="text-2xl">←</a>
            <h2 class="truncate font-semibold"><%= info.title %></h2>
        </div>

        <!-- Video Player (Vertical Style) -->
        <div class="flex-grow bg-gray-900 relative">
            <video 
                src="<%= info.videoUrl %>" 
                class="w-full h-full object-contain"
                controls 
                autoplay
            ></video>
        </div>

        <!-- Detail Drama -->
        <div class="p-4 bg-zinc-900 rounded-t-3xl mt-[-20px] z-10">
            <h1 class="text-xl font-bold"><%= info.title %></h1>
            <p class="text-gray-400 text-sm mt-2 line-clamp-3">
                <%= info.description || "Tidak ada deskripsi untuk drama ini." %>
            </p>
            
            <div class="mt-6">
                <h4 class="font-bold mb-2">Episodes</h4>
                <div class="flex gap-2 overflow-x-auto pb-4">
                    <% for(let i=1; i<=10; i++) { %>
                        <button class="min-w-[50px] h-[50px] bg-gray-800 rounded-lg border border-gray-700 hover:bg-pink-600">
                            <%= i %>
                        </button>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
